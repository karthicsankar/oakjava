#include <stdio.h>
int main()
{
    int num,in,factorial;
printf("Enter a number");
    scanf("%d",&num);
    {
for(in=1;in<=num;++in)
        {
 factorial*=in;              
        }
        }
        printf("Factorial of %d=%d",n,factorial);
    }
