#include<stdio.h>
int main()
{
	int in,cn=0;
	char k[100];
	for(in=0;in<=50;in++)
	{
		scanf("%c",&k[in]);
	}
	for(in=0;in<=k[in];in++)
	{
		if((k[in]>='a'&&k[in]<='z')||(k[in]>='A'&&k[in]<='Z'))
		{
		c++;
	}}
	printf("%d",c);
return 0;
}
